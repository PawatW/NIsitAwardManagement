name: UAT - Build & Deploy Backend

on:
  push:
    branches: [ "develop" ]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: uat-backend-develop
  cancel-in-progress: true

jobs:
  build-deploy:
    runs-on: self-hosted
    timeout-minutes: 30

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Login to GHCR
        run: |
          echo "${{ secrets.TOKEN_SECRET }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Build Docker image
        run: |
          IMAGE=ghcr.io/472-68-agiledevops/p2-final-project-backend-team
          SHA_TAG=uat-${{ github.sha }}
          ENV_TAG=uat
          docker build -t $IMAGE:$ENV_TAG .

      - name: Push image
        run: |
          IMAGE=ghcr.io/472-68-agiledevops/p2-final-project-backend-team
          SHA_TAG=uat-${{ github.sha }}
          ENV_TAG=uat
          docker push $IMAGE:$ENV_TAG

      - name: Deploy to UAT
        run: |
          IMAGE=ghcr.io/472-68-agiledevops/p2-final-project-backend-team
          SHA_TAG=uat-${{ github.sha }}
          ENV_TAG=uat
          kubectl set image deployment/backend backend=$IMAGE:$ENV_TAG
          kubectl rollout status deployment/backend --timeout=180s